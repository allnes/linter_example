language: cpp

jobs:
  include:
    - os: linux
      dist: bionic
      addons:
        apt:
          sources: ubuntu-toolchain-r-test
          packages: g++-8

install:
- sudo apt-get install python3-pip;
- python3 -m pip install --upgrade pip;
- python3 -m install setuptools cpplint;

script:
- mkdir build
- cd build
- cmake ..
- cmake --build . --config -j4
- ./linter_example 