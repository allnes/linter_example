language: cpp

jobs:
  include:
    - os: linux
      dist: bionic
      addons:
        apt:
          sources: ubuntu-toolchain-r-test
          packages: g++-8

script:
- mkdir build
- cd build
- cmake ..
- cmake --build . --config -j4
- ./linter_example 